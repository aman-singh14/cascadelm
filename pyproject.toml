[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cascadelm"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.0",
    "python-dotenv>=1.0"
]